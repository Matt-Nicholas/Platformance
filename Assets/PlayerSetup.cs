using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSetup : MonoBehaviour {
  int playersJoined = 1;
  int playersReady;
  //TheGameManager GM = TheGameManager.Instance;

  private GameObject selectorTwo;
  private GameObject playerTwoJoin;

  // Use this for initialization
  void Start() {
    //selectorTwo = transform.GetChild(1).gameObject;
    //playerTwoJoin = transform.GetChild(2).gameObject;
  }

  // Update is called once per frame
  void Update() {
    //if(GM.currentMode == TheGameManager.GameMode.SinglePlayer) {

    //}
    //else{
    //if(playersJoined < 2) {
    //  if(InputManager.AButton(2)) {
    //    playerTwoJoin.SetActive(false);
    //    selectorTwo.SetActive(true);
    //  }
      //}
    //}
  }
}
